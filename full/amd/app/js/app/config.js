define([], function () {
  // Configuration
  // -------------
  // Stores all application configuration.
  var config = {
    // Local Storage Name
    storeName: "notes-amd"
  };

  return config;
});
